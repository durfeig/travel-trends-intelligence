name: Collect Travel Trends

on:
  schedule:
    # Corre 2 veces al día: 6am y 6pm UTC
    - cron: '0 6,18 * * *'
  # Disparo desde el botón del dashboard (vía API de GitHub)
  repository_dispatch:
    types: [manual_collect]
  # Permite disparo manual desde GitHub Actions UI
  workflow_dispatch:
    inputs:
      mode:
        description: 'Collection mode'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - trending
          - rising
          - monitored

permissions:
  contents: read

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collector/requirements.txt

      - name: Install dependencies
        run: pip install -r collector/requirements.txt

      - name: Run trend collector
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          cd collector
          python collect_trends.py --mode ${{ github.event.inputs.mode || github.event.client_payload.mode || 'all' }}

      - name: Run Wikipedia collector
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          cd collector
          python wikipedia_collector.py

      - name: Run Travelpayouts collector
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          TRAVELPAYOUTS_TOKEN: ${{ secrets.TRAVELPAYOUTS_TOKEN }}
        run: |
          cd collector
          python travelpayouts_collector.py

      - name: Run analyzer
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          cd collector
          python analyzer.py
